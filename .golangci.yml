version: "2"
linters:
  enable:
    - sloglint
    - staticcheck
    - gosec
    - prealloc
    - goconst
    - unconvert
    - misspell
    - unparam
    - nakedret
    - dupl

  settings:
    sloglint:
      no-global: "all"
      static-msg: true
      attr-only: true
      msg-style: "lowercased"
      key-naming-case: "camel"
      args-on-sep-lines: true
      forbidden-keys:
        - "time"
        - "level"
        - "msg"
        - "source"
    errcheck:
      check-type-assertions: true

    dupl:
      threshold: 100

formatters:
  enable:
    - goimports
    - gofumpt
  settings:
    gofumpt:
      extra-rules: true
      module-path: "card-watcher"
    goimports:
      local-prefixes:
        - "card-watcher"
